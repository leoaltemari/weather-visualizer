export const environment = Object.freeze({
  production: false,
  buienradarApiUrl: 'https://weather-api-kx46.onrender.com',
  /** Change to it, if you are running the GraphQL API locally */
  // buienradarApiUrl: 'http://localhost:4000',
});
